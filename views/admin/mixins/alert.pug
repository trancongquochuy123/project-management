mixin alert-success(time)
    - if (messages.success)
        .message.success
        div(
            class="alert alert-success"
            show-alert
            data-time="5000"
        ) #{messages.success} <span class="close-alert" close-alert>×</span>
